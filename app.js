const рядок1 = prompt("Введіть перший рядок:");
const рядок2 = prompt("Введіть другий рядок:");
const рядок3 = prompt("Введіть третій рядок:");

const result = рядок3.concat(рядок1, рядок2);

alert (result);

console.log({ result });

const number = prompt("Введіть п'ятизначне число:");

const result2 = number.split('').join(' ');

alert (result2);

console.log({ result2 });